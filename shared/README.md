Optional shared types/schemas (e.g. TypeScript or JSON schema) can go here. Not required for the minimal scaffold.

- `value-chart-types.ts` contains `ValueChartPoint` and `ValueChartResponse` used by the demo value-chart feature.
